# Руководство по внесению вклада

Спасибо за интерес к проекту! Мы рады любому вкладу — от исправления опечаток до разработки новых функций.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [С чего начать](#с-чего-начать)
- [Настройка окружения](#настройка-окружения)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кодирования](#стандарты-кодирования)
- [Отправка Pull Request](#отправка-pull-request)
- [Сообщения о проблемах](#сообщения-о-проблемах)

## Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь соблюдать наш [Кодекс поведения](CODE_OF_CONDUCT.md). Пожалуйста, ознакомьтесь с ним перед началом работы.

## С чего начать

### Для новичков в проекте

1. **Ознакомьтесь с документацией** - прочитайте [README.md](README.md), чтобы понять архитектуру проекта
2. **Изучите открытые issues** - найдите задачи с метками `good first issue` или `help wanted`
3. **Задайте вопросы** - не стесняйтесь спрашивать в issues, если что-то непонятно

### Типы вклада

Мы принимаем следующие типы вклада:

- 🐛 Исправления багов
- ✨ Новые функции
- 📝 Улучшение документации
- 🎨 Улучшение кода (рефакторинг, оптимизация)
- ✅ Написание тестов
- 🌍 Переводы

## Настройка окружения

### Требования

- Python 3.11 или выше
- Docker и docker-compose (опционально, для тестирования)
- Git

### Шаг 1: Форк и клонирование

1. Создайте форк репозитория на GitHub
2. Клонируйте ваш форк:

```bash
git clone https://github.com/ваш-username/max-telegram-bridge.git
cd max-telegram-bridge
```

3. Добавьте upstream remote:

```bash
git remote add upstream https://github.com/original-owner/max-telegram-bridge.git
```

### Шаг 2: Создание виртуального окружения

```bash
python3 -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate
```

### Шаг 3: Установка зависимостей

```bash
# Основные зависимости
pip install -r requirements.txt

# Зависимости для разработки
pip install -e ".[dev]"
```

### Шаг 4: Настройка pre-commit хуков

```bash
pre-commit install
```

Теперь перед каждым коммитом будут автоматически запускаться линтеры и форматтеры.

### Шаг 5: Настройка .env для разработки

```bash
cp .env.example .env
# Отредактируйте .env и заполните необходимые переменные
```

### Шаг 6: Проверка установки

```bash
# Запуск сервера
python -m uvicorn app.main:app --reload

# В другом терминале проверьте health endpoint
curl http://localhost:8000/health
```

## Процесс разработки

### 1. Создайте новую ветку

Всегда создавайте новую ветку для ваших изменений:

```bash
git checkout -b feature/краткое-описание
# или
git checkout -b fix/описание-бага
```

Именование веток:
- `feature/` - для новых функций
- `fix/` - для исправления багов
- `docs/` - для изменений в документации
- `refactor/` - для рефакторинга
- `test/` - для добавления тестов

### 2. Внесите изменения

- Пишите чистый, понятный код
- Следуйте [стандартам кодирования](#стандарты-кодирования)
- Добавляйте docstrings к функциям и классам
- Пишите тесты для нового функционала (когда инфраструктура тестирования будет готова)

### 3. Проверьте свой код

```bash
# Форматирование кода
black app/

# Линтинг
ruff check app/

# Проверка типов
mypy app/

# Запуск тестов (когда они будут добавлены)
pytest
```

### 4. Коммит изменений

```bash
git add .
git commit -m "Краткое описание изменений

Более подробное описание, если необходимо.
Укажите номер issue, если есть: #123"
```

**Правила для commit messages:**
- Используйте настоящее время ("Добавляет функцию", а не "Добавил функцию")
- Первая строка — краткое описание (до 72 символов)
- Более подробное описание (при необходимости) после пустой строки
- Ссылайтесь на issue: `Closes #123`, `Fixes #456`, `Relates to #789`

### 5. Синхронизация с upstream

Перед отправкой PR синхронизируйте вашу ветку с основной веткой:

```bash
git fetch upstream
git rebase upstream/main
```

### 6. Push в ваш форк

```bash
git push origin ваша-ветка
```

## Стандарты кодирования

### Python стиль

Мы следуем [PEP 8](https://pep8.org/) с некоторыми дополнениями:

- **Максимальная длина строки**: 100 символов
- **Форматирование**: Black (автоматически)
- **Линтинг**: Ruff
- **Проверка типов**: MyPy

### Структура кода

```python
"""Модуль для работы с X.

Более подробное описание модуля.
"""

from typing import Optional

import aiohttp
from fastapi import HTTPException

from app.config import settings


class ExampleClass:
    """Краткое описание класса.

    Attributes:
        attr1: Описание атрибута
        attr2: Описание атрибута
    """

    def __init__(self, param: str) -> None:
        """Инициализация класса.

        Args:
            param: Описание параметра
        """
        self.attr1 = param

    async def example_method(self, value: int) -> Optional[str]:
        """Краткое описание метода.

        Args:
            value: Описание параметра

        Returns:
            Описание возвращаемого значения

        Raises:
            HTTPException: Когда происходит ошибка
        """
        if value < 0:
            raise HTTPException(status_code=400, detail="Value must be positive")

        return f"Result: {value}"
```

### Type hints

Используйте type hints везде, где это возможно:

```python
from typing import Optional, List, Dict

async def fetch_data(
    url: str,
    timeout: int = 30,
    headers: Optional[Dict[str, str]] = None
) -> List[Dict[str, any]]:
    """Получает данные из API."""
    ...
```

### Логирование

Используйте встроенный модуль логирования:

```python
import logging

logger = logging.getLogger(__name__)

logger.info("Информационное сообщение")
logger.warning("Предупреждение")
logger.error("Ошибка", exc_info=True)
```

### Обработка ошибок

```python
from fastapi import HTTPException

try:
    result = await some_operation()
except ValueError as e:
    logger.error(f"Invalid value: {e}")
    raise HTTPException(status_code=400, detail=str(e))
except Exception as e:
    logger.exception("Unexpected error occurred")
    raise HTTPException(status_code=500, detail="Internal server error")
```

## Отправка Pull Request

### Перед отправкой

Убедитесь, что:

- [ ] Код отформатирован с помощью `black`
- [ ] Линтинг проходит без ошибок (`ruff check`)
- [ ] Проверка типов успешна (`mypy`)
- [ ] Все тесты проходят (когда они будут добавлены)
- [ ] Документация обновлена (если необходимо)
- [ ] CHANGELOG.md обновлён (для значительных изменений)

### Создание PR

1. Перейдите на GitHub и создайте Pull Request из вашей ветки в `main`
2. Заполните шаблон PR:
   - Опишите, что изменено
   - Укажите, какую проблему решает PR
   - Добавьте скриншоты (если применимо)
3. Свяжите PR с issue (если есть)
4. Дождитесь review

### После создания PR

- Отслеживайте комментарии reviewers
- Вносите изменения по замечаниям
- Отвечайте на вопросы
- Будьте терпеливы — review может занять время

### Автоматические проверки

После создания PR автоматически запустятся:

- ✅ Линтинг и форматирование
- ✅ Проверка типов
- ✅ Тесты (когда будут добавлены)
- ✅ Сборка Docker образа

Убедитесь, что все проверки проходят успешно.

## Сообщения о проблемах

### Как сообщить о баге

1. Проверьте, что баг ещё не зарепорчен в [Issues](https://github.com/noshackleshot/telegram-max-bridge/issues)
2. Создайте новый issue, используя шаблон Bug Report
3. Укажите:
   - Описание проблемы
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Версию Python, Docker
   - Логи (если есть)

### Предложение новой функции

1. Проверьте, что функция не была предложена ранее
2. Создайте новый issue, используя шаблон Feature Request
3. Опишите:
   - Какую проблему решает функция
   - Как она должна работать
   - Примеры использования
   - Альтернативные решения (если есть)

## Вопросы?

Если у вас есть вопросы:

- Создайте [Discussion](https://github.com/noshackleshot/telegram-max-bridge/discussions)
- Задайте вопрос в issue
- Свяжитесь с мейнтейнерами

---

**Спасибо за ваш вклад в проект!** 🎉
