# Max to Telegram Bridge

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)](https://www.python.org/downloads/)
[![Docker](https://img.shields.io/badge/docker-%230db7ed.svg?logo=docker&logoColor=white)](https://www.docker.com/)
[![CI](https://github.com/noshackleshot/telegram-max-bridge/workflows/CI/badge.svg)](https://github.com/noshackleshot/telegram-max-bridge/actions)
[![Code style: black](https://img.shields.io/badge/code%20style-black-000000.svg)](https://github.com/psf/black)

Синхронизация сообщений между мессенджером Max и Telegram через GREEN-API.

## Возможности

- **Гибкая односторонняя синхронизация**: Max → Telegram или Telegram → Max
- Пересылка текстовых сообщений
- Поддержка медиа: изображения, видео, документы, аудио
- Гибкое управление направлением через флаги в .env
- Фильтрация по конкретным чатам (опционально)
- Отображение информации об отправителе
- Контейнеризация через Docker для простого деплоя
- Логирование и обработка ошибок
- Health checks и автоматический перезапуск

## Архитектура

```
┌─────────────────────────────────────────────────┐
│                                                 │
│  Max (личный аккаунт) ←──────────────────┐     │
│         │                                 │     │
│         │ GREEN-API webhook               │     │
│         ↓                                 │     │
│  ┌──────────────────────────────────────┐ │     │
│  │     FastAPI Server (этот проект)     │ │     │
│  │                                      │ │     │
│  │  • /webhook (Max → Telegram)        │ │     │
│  │  • /telegram/webhook (Telegram → Max)│ │     │
│  └──────────────────────────────────────┘ │     │
│         │                                 │     │
│         │ Telegram Bot API                │     │
│         ↓                         GREEN-API     │
│  Telegram Channel/Chat ──────────────────┘     │
│                                                 │
└─────────────────────────────────────────────────┘
```

## Требования

- Docker и docker-compose
- Аккаунт GREEN-API (для подключения Max)
- Telegram Bot Token (получить у @BotFather)
- Telegram канал или группа

## Быстрый старт

### 1. Настройка GREEN-API (Max)

1. Перейдите на https://green-api.com/
2. Зарегистрируйтесь и создайте инстанс
3. Подключите свой номер телефона Max:
   - Сканируйте QR-код через приложение Max
   - Или введите код подтверждения
4. Сохраните:
   - `Instance ID` (идентификатор инстанса)
   - `API Token` (токен API)

### 2. Создание Telegram бота

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и придумайте имя
4. Сохраните токен бота (формат: `1234567890:ABCdefGHIjklMNOpqrs`)
5. Добавьте бота в ваш канал как администратора
6. Узнайте ID канала:
   - Для публичных каналов: `@channel_name`
   - Для приватных: используйте @userinfobot

### 3. Узнайте Chat ID в Max (опционально)

Если хотите пересылать сообщения только из конкретного чата:

1. Используйте GREEN-API метод [GetChats](https://green-api.com/en/docs/api/service/GetChats/)
2. Chat ID - это числовой идентификатор:
   - Личные чаты: положительное число (например, `16958332`)
   - Групповые чаты: отрицательное число (например, `-69020002426896`)
3. Можно оставить пустым для пересылки всех сообщений

**Пример получения списка чатов:**
```bash
curl "https://3100.api.green-api.com/v3/waInstance{INSTANCE_ID}/getChats/{API_TOKEN}"
```

### 4. Настройка проекта

Клонируйте или скачайте проект:

```bash
cd /path/to/max-to-telegram-bridge
```

Создайте файл `.env` из примера:

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните данные:

```env
# GREEN-API
MAX_INSTANCE_ID=your_instance_id
MAX_API_TOKEN=your_api_token

# Telegram Bot
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrs

# Управление направлением синхронизации (ТОЛЬКО ОДНО может быть true!)
ENABLE_MAX_TO_TELEGRAM=true    # Max → Telegram
ENABLE_TELEGRAM_TO_MAX=false   # Telegram → Max

# === Настройки Max → Telegram ===
TELEGRAM_CHANNEL_ID=@your_channel  # Куда отправлять сообщения из Max
MAX_CHAT_ID=                       # (Опционально) Фильтр чата Max

# === Настройки Telegram → Max ===
MAX_TARGET_CHAT_ID=-69020002426896  # Куда отправлять (числовой chatId из GREEN-API)
TELEGRAM_CHAT_ID=                   # (Опционально) Фильтр чата Telegram

# Telegram Webhook (для направления Telegram → Max)
TELEGRAM_WEBHOOK_URL=https://your-domain.com/telegram/webhook
TELEGRAM_WEBHOOK_SECRET=your-secret-token

# Сервер
WEBHOOK_PORT=8000
LOG_LEVEL=INFO
```

**Примеры конфигураций:**

1. **Только Max → Telegram** (по умолчанию):
   ```env
   ENABLE_MAX_TO_TELEGRAM=true
   ENABLE_TELEGRAM_TO_MAX=false
   TELEGRAM_CHANNEL_ID=@my_channel
   ```

2. **Только Telegram → Max**:
   ```env
   ENABLE_MAX_TO_TELEGRAM=false
   ENABLE_TELEGRAM_TO_MAX=true
   MAX_TARGET_CHAT_ID=-69020002426896
   TELEGRAM_WEBHOOK_URL=https://your-domain.com/telegram/webhook
   ```

> **⚠️ ВАЖНО:** Нельзя включать оба направления одновременно! Это приведёт к бесконечному дублированию сообщений. Приложение не запустится, если оба флага `ENABLE_MAX_TO_TELEGRAM` и `ENABLE_TELEGRAM_TO_MAX` установлены в `true`.

### 5. Запуск с Docker

Соберите и запустите контейнер:

```bash
docker-compose up -d --build
```

Проверьте статус:

```bash
docker-compose ps
docker-compose logs -f
```

Проверьте health:

```bash
curl http://localhost:8000/health
```

### 6. Настройка Webhooks

Для работы бриджа нужно настроить вебхуки в обоих направлениях.

#### 6.1 Webhook для GREEN-API (Max → Telegram)

Если вы включили `ENABLE_MAX_TO_TELEGRAM=true`, нужно указать GREEN-API, куда отправлять уведомления о новых сообщениях.

**Способ 1: Через личный кабинет GREEN-API**

1. Войдите в личный кабинет https://console.green-api.com/
2. Выберите ваш инстанс
3. Перейдите в "Настройки" → "Webhook"
4. Укажите URL вашего сервера:
   ```
   http://your-server-ip:8000/webhook
   ```
   или для production:
   ```
   https://your-domain.com/webhook
   ```
5. Включите отправку уведомлений

**Способ 2: Через API**

```bash
curl -X POST \
  "https://api.green-api.com/waInstance{INSTANCE_ID}/SetSettings/{API_TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{
    "webhookUrl": "http://your-server-ip:8000/webhook",
    "webhookUrlToken": "",
    "incomingWebhook": "yes"
  }'
```

#### 6.2 Webhook для Telegram (Telegram → Max)

Если вы включили `ENABLE_TELEGRAM_TO_MAX=true`, webhook регистрируется **автоматически** при старте приложения, если указан `TELEGRAM_WEBHOOK_URL` в `.env`.

**Автоматическая регистрация:**
- Приложение само зарегистрирует webhook при запуске
- URL берется из переменной `TELEGRAM_WEBHOOK_URL`
- При остановке приложения webhook автоматически удаляется

**Ручная регистрация (если нужно):**

```bash
curl -X POST \
  "https://api.telegram.org/bot{BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://your-domain.com/telegram/webhook",
    "secret_token": "your-secret-token"
  }'
```

**Проверка статуса webhook:**

```bash
curl "https://api.telegram.org/bot{BOT_TOKEN}/getWebhookInfo"
```

#### Важно для локальной разработки

Если ваш сервер находится за NAT или на локальной машине, используйте туннель:

**Вариант 1: ngrok**
```bash
ngrok http 8000
# Используйте URL вида: https://abc123.ngrok.io/webhook
```

**Вариант 2: localtunnel**
```bash
npx localtunnel --port 8000
```

**Вариант 3: Cloudflare Tunnel**
```bash
cloudflared tunnel --url http://localhost:8000
```

Затем укажите полученный публичный URL в:
- GREEN-API настройках: `https://your-tunnel.com/webhook`
- `.env` файле: `TELEGRAM_WEBHOOK_URL=https://your-tunnel.com/telegram/webhook`

## Использование на production сервере

### На VPS/Dedicated сервере

1. Установите Docker и docker-compose:
   ```bash
   # Ubuntu/Debian
   sudo apt update
   sudo apt install docker.io docker-compose
   sudo systemctl enable docker
   sudo systemctl start docker
   ```

2. Скопируйте проект на сервер:
   ```bash
   scp -r max-to-telegram-bridge user@your-server:/opt/
   ```

3. Настройте и запустите:
   ```bash
   cd /opt/max-to-telegram-bridge
   nano .env  # Заполните настройки
   docker-compose up -d
   ```

4. Настройте автозапуск:
   ```bash
   sudo systemctl enable docker
   ```

### С Nginx (рекомендуется для production)

Создайте конфигурацию Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Для HTTPS используйте Let's Encrypt:

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

## Тестирование

Отправьте тестовое сообщение:

```bash
curl -X POST http://localhost:8000/test \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Test message from Max bridge",
    "sender_name": "Test User"
  }'
```

## Мониторинг

Просмотр логов:

```bash
docker-compose logs -f
```

Проверка здоровья:

```bash
curl http://localhost:8000/health
```

Перезапуск:

```bash
docker-compose restart
```

Остановка:

```bash
docker-compose down
```

## Структура проекта

```
max-to-telegram-bridge/
├── app/
│   ├── __init__.py
│   ├── main.py                # FastAPI сервер
│   ├── config.py              # Конфигурация с флагами направлений
│   ├── handlers.py            # Обработчики MAX → Telegram
│   ├── telegram_handlers.py   # Обработчики Telegram → Max
│   ├── telegram_client.py     # Telegram Bot клиент
│   └── green_api_client.py    # GREEN-API клиент
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── .env.example
├── .gitignore
├── README.md
└── CLAUDE.md
```

## Troubleshooting

### Общие проблемы

#### Docker не запускается

```bash
# Проверьте логи
docker-compose logs

# Пересоберите контейнер
docker-compose down
docker-compose up -d --build
```

#### Проверка статуса направлений

```bash
# Проверьте health endpoint для просмотра статуса
curl http://localhost:8000/health | jq
```

Ответ покажет статус каждого направления:
```json
{
  "status": "healthy",
  "directions": {
    "max_to_telegram": true,
    "telegram_to_max": true
  }
}
```

### Max → Telegram (не работает)

#### Сообщения не приходят в Telegram

1. Проверьте, что `ENABLE_MAX_TO_TELEGRAM=true`
2. Убедитесь, что бот добавлен в канал как администратор
3. Проверьте правильность `TELEGRAM_CHANNEL_ID`
4. Проверьте логи: `docker-compose logs -f`

#### GREEN-API webhook не срабатывает

1. Убедитесь, что webhook URL доступен извне
2. Проверьте настройки в GREEN-API консоли
3. Используйте ngrok для локальной разработки
4. Проверьте логи GREEN-API: https://console.green-api.com/

#### Ошибка авторизации GREEN-API

1. Проверьте правильность `MAX_INSTANCE_ID` и `MAX_API_TOKEN`
2. Убедитесь, что инстанс активен в GREEN-API
3. Проверьте, не истек ли срок действия API токена

### Telegram → Max (не работает)

#### Сообщения из Telegram не приходят в Max

1. Проверьте, что `ENABLE_TELEGRAM_TO_MAX=true`
2. Убедитесь, что указан `TELEGRAM_WEBHOOK_URL`
3. Проверьте, что webhook зарегистрирован:
   ```bash
   curl "https://api.telegram.org/bot{BOT_TOKEN}/getWebhookInfo"
   ```
4. Проверьте правильность `MAX_TARGET_CHAT_ID` (числовой chatId из GREEN-API, например: -69020002426896)
5. Проверьте логи: `docker-compose logs -f | grep telegram`

#### Telegram webhook возвращает ошибку

1. Убедитесь, что URL доступен извне (проверьте через curl)
2. Проверьте совпадение `TELEGRAM_WEBHOOK_SECRET`
3. Убедитесь, что используется HTTPS (Telegram требует HTTPS для production)
4. Для локальной разработки используйте ngrok

#### Ошибка отправки в Max через GREEN-API

1. Проверьте формат `MAX_TARGET_CHAT_ID` (числовой chatId из GREEN-API)
2. Получите правильный chatId через API: `curl "https://3100.api.green-api.com/v3/waInstance{INSTANCE_ID}/getChats/{API_TOKEN}"`
3. Проверьте, что GREEN-API инстанс авторизован
4. Проверьте логи для деталей ошибки: `docker-compose logs -f`

### Проблемы с фильтрацией

#### Пересылаются сообщения из всех чатов

1. Установите `MAX_CHAT_ID` для фильтрации Max → Telegram
2. Установите `TELEGRAM_CHAT_ID` для фильтрации Telegram → Max
3. Перезапустите контейнер: `docker-compose restart`

#### Не пересылаются сообщения из нужного чата

1. Проверьте правильность ID чата
2. Для Max: используйте числовой chatId из GREEN-API (например, 16958332 или -69020002426896)
3. Для Telegram: используйте числовой ID (например, -1001234567890)
4. Проверьте логи, там видно какие чаты фильтруются

## Безопасность

- Храните `.env` в секрете, не коммитьте в Git
- Используйте HTTPS для webhook в production
- Настройте `WEBHOOK_SECRET` для дополнительной защиты
- Ограничьте доступ к серверу через firewall
- Регулярно обновляйте зависимости

## Как внести вклад

Мы приветствуем вклад от сообщества! Вот как вы можете помочь:

- 🐛 **Сообщите о баге** - используйте [шаблон Bug Report](https://github.com/noshackleshot/telegram-max-bridge/issues/new?template=bug_report.md)
- ✨ **Предложите функцию** - используйте [шаблон Feature Request](https://github.com/noshackleshot/telegram-max-bridge/issues/new?template=feature_request.md)
- 📖 **Улучшите документацию** - исправьте опечатки, добавьте примеры
- 💻 **Отправьте Pull Request** - прочитайте [руководство для контрибьюторов](CONTRIBUTING.md)

### Быстрый старт для разработки

```bash
# Форк и клонирование
git clone https://github.com/your-username/telegram-max-bridge.git
cd telegram-max-bridge

# Установка зависимостей для разработки
python -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate
pip install -r requirements-dev.txt

# Установка pre-commit хуков
pre-commit install

# Создание ветки для изменений
git checkout -b feature/my-feature

# После изменений - проверка кода
black app/
ruff check app/
mypy app/
```

Подробнее читайте в [CONTRIBUTING.md](CONTRIBUTING.md).

## Лицензия

Этот проект лицензирован под лицензией MIT - подробности в файле [LICENSE](LICENSE).

## Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что все переменные окружения заполнены
3. Проверьте раздел [Troubleshooting](#troubleshooting) в этом README
4. Проверьте документацию GREEN-API: https://green-api.com/docs/
5. Создайте [issue](https://github.com/noshackleshot/telegram-max-bridge/issues) с подробным описанием проблемы

## Авторы и благодарности

- Благодарим всех [контрибьюторов](https://github.com/noshackleshot/telegram-max-bridge/graphs/contributors) проекта

---

⭐ Если проект оказался полезным, поставьте звезду на GitHub!
